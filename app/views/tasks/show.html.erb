<h1> <%= @task.title %> </h1>

<%= link_to 'Edit', edit_path(@task), class: 'btn btn-secondary btn-sm' %>

<%= link_to '🗑', delete_path(@task), class: 'btn btn-danger btn-sm', method: :delete, data: { confirm: 'Are you sure?' } %>


<h2>
  <%= @task.details  %>
</h2>

<%= link_to "Tasks List", tasks_path %>

